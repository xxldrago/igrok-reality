# A generic, single database configuration.

[alembic]
# path to migration scripts (%(here)s = directory of this ini file,
# so the command works from any working directory)
script_location = %(here)s/alembic

# sys.path prepend (relative to this file)
prepend_sys_path = .

# sqlalchemy.url is intentionally empty — env.py reads from app.shared.config
sqlalchemy.url =

[loggers]
keys = root,sqlalchemy,alembic

[handlers]
keys = console

[formatters]
keys = generic

[logger_root]
level = WARN
handlers = console
qualname =

[logger_sqlalchemy]
level = WARN
handlers =
qualname = sqlalchemy.engine

[logger_alembic]
level = INFO
handlers =
qualname = alembic

[handler_console]
class = StreamHandler
args = (sys.stderr,)
level = NOTSET
formatter = generic

[formatter_generic]
format = %(levelname)-5.5s [%(name)s] %(message)s
datefmt = %H:%M:%S
