---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - Dockerfile
  - pyproject.toml
  - .env.example
  - .gitignore
  - src/app/__init__.py
  - src/app/shared/__init__.py
  - src/app/shared/config.py
autonomous: true
requirements: []

estimate:
  tokens: 15000
  raw_tokens: 15000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Developer can run docker compose up and all 6 services start without errors"
    - "PostgreSQL accepts connections and Redis responds to PING"
    - "Environment variables are loaded from .env via pydantic-settings"
  artifacts:
    - path: "docker-compose.yml"
      provides: "6-service orchestration (bot, api, worker, scheduler, postgres, redis)"
      contains: "services:"
    - path: "Dockerfile"
      provides: "Python 3.11+ multi-stage build"
      contains: "FROM python"
    - path: "pyproject.toml"
      provides: "All Phase 1 dependencies declared"
      contains: "dependencies"
    - path: "src/app/shared/config.py"
      provides: "Pydantic Settings class with all env vars"
      contains: "class Settings"
  key_links:
    - from: "docker-compose.yml"
      to: "Dockerfile"
      via: "build context for bot/api/worker/scheduler services"
      pattern: "build:"
    - from: "docker-compose.yml"
      to: ".env.example"
      via: "env_file directive references .env"
      pattern: "env_file"
    - from: "src/app/shared/config.py"
      to: ".env.example"
      via: "Settings class reads from environment matching .env keys"
      pattern: "class Settings"
---

<objective>
Scaffold the project with Docker Compose, Dockerfile, pyproject.toml, environment config, and shared kernel settings.

Purpose: Every subsequent plan depends on this foundation — services must start, database must be reachable, and configuration must be loadable.
Output: A runnable `docker compose up` that starts PostgreSQL, Redis, and placeholder processes for bot, API, worker, scheduler.
</objective>

<execution_context>
@/Users/alekseimikhalkin/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/alekseimikhalkin/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Docker infrastructure and pyproject.toml</name>
  <files>docker-compose.yml, Dockerfile, pyproject.toml, .gitignore, .env.example</files>
  <action>
Create the following files in the project root:

1. `pyproject.toml` with project metadata and ALL dependencies for Phase 1-7:
   - Core: python>=3.11, aiogram>=3.31, fastapi>=0.138, uvicorn[standard], pydantic>=2.13, pydantic-settings>=2.0
   - Database: sqlalchemy[asyncio]>=2.0, asyncpg>=0.30, alembic>=1.13
   - Task queue: arq>=0.28, redis>=5.0, apscheduler>=3.11
   - HTTP: httpx, python-multipart
   - Dev: pytest, pytest-asyncio, ruff
   - Set project name = "igrok-reality", version = "0.1.0"

2. `Dockerfile` — Python 3.11-slim, install system deps (gcc, libpq-dev), copy pyproject.toml, pip install in virtualenv, copy src/, expose nothing (processes have different entrypoints)

3. `docker-compose.yml` with 6 services:
   - `postgres`: image postgres:15-alpine, port 5432, volume for data persistence, healthcheck (pg_isready)
   - `redis`: image redis:7-alpine, port 6379, healthcheck (redis-cli ping)
   - `bot`: build from Dockerfile, command `python -m app.bot.main`, depends_on postgres+redis (condition: service_healthy), env_file .env
   - `api`: build from Dockerfile, command `python -m uvicorn app.api.main:app --host 0.0.0.0 --port 8000`, depends_on postgres+redis, ports 8000:8000, env_file .env
   - `worker`: build from Dockerfile, command `python -m app.worker.main`, depends_on postgres+redis, env_file .env
   - `scheduler`: build from Dockerfile, command `python -m app.scheduler.main`, depends_on postgres+redis, env_file .env

4. `.env.example` with all required env vars documented:
   - BOT_TOKEN, MASTER_CHANNEL_ID, QUEST_CHANNEL_ID, PAYMENT_CHANNEL_ID
   - DATABASE_URL=postgresql+asyncpg://igrok:igrok@postgres:5432/igrok
   - REDIS_URL=redis://redis:6379/0
   - PLATEGA_MERCHANT_ID, PLATEGA_SECRET, PLATEGA_WEBHOOK_URL
   - ENVIRONMENT=development, LOG_LEVEL=INFO, TZ=Europe/Moscow

5. `.gitignore` for Python + Docker + IDE: __pycache__, .env, *.pyc, .venv, node_modules, .planning/

Use docker-compose.yml v2 format (no `version:` key).
  </action>
  <verify>
    <automated>docker compose config --quiet && echo "compose valid" || echo "compose invalid"</automated>
  </verify>
  <done>docker-compose.yml parses without error, Dockerfile exists with FROM python, pyproject.toml has all dependencies, .env.example documents all vars, .gitignore covers Python artifacts.</done>
</task>

<task type="auto">
  <name>Task 2: Shared kernel config and process stubs</name>
  <files>src/app/__init__.py, src/app/shared/__init__.py, src/app/shared/config.py, src/app/bot/__init__.py, src/app/bot/main.py, src/app/api/__init__.py, src/app/api/main.py, src/app/worker/__init__.py, src/app/worker/main.py, src/app/scheduler/__init__.py, src/app/scheduler/main.py</files>
  <action>
Create the shared kernel and process entrypoint stubs:

1. `src/app/__init__.py` — empty
2. `src/app/shared/__init__.py` — empty
3. `src/app/shared/config.py` — Pydantic BaseSettings class:
   - Fields: BOT_TOKEN (str), MASTER_CHANNEL_ID (int), QUEST_CHANNEL_ID (int), PAYMENT_CHANNEL_ID (int), DATABASE_URL (str), REDIS_URL (str), PLATEGA_MERCHANT_ID (str), PLATEGA_SECRET (str), PLATEGA_WEBHOOK_URL (str), ENVIRONMENT (str = "development"), LOG_LEVEL (str = "INFO"), TZ (str = "Europe/Moscow")
   - model_config: env_file=".env", env_file_encoding="utf-8"
   - Export a module-level `settings = Settings()` instance

4. Process stubs — each is a minimal async main that logs startup:
   - `src/app/bot/main.py`: async def main() with print("Bot starting..."), if __name__ == "__main__": asyncio.run(main())
   - `src/app/api/main.py`: FastAPI app instance with GET /health returning {"status": "ok"}, uvicorn import for standalone run
   - `src/app/worker/main.py`: async def main() with print("Worker starting..."), if __name__ == "__main__": asyncio.run(main())
   - `src/app/scheduler/main.py`: async def main() with print("Scheduler starting..."), if __name__ == "__main__": asyncio.run(main())

All __init__.py files are empty. All process stubs import asyncio and have proper if __name__ guards.
  </action>
  <verify>
    <automated>cd "/Users/alekseimikhalkin/Documents/BOT UILVIYA" && python -c "from app.shared.config import settings; print(f'Config loaded: ENV={settings.ENVIRONMENT}')"</automated>
  </verify>
  <done>Settings class loads from .env, all 4 process files exist with async main(), FastAPI app exposes /health endpoint.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Environment variables → Config | Secrets (BOT_TOKEN, DB passwords) loaded from .env, must not be logged |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Information Disclosure | config.py | medium | mitigate | Never log Settings instance; use structlog with redaction for sensitive fields |
| T-01-02 | Tampering | docker-compose.yml | low | accept | Local dev only; production uses separate compose override |
</threat_model>

<verification>
1. `docker compose config` validates without error
2. `python -c "from app.shared.config import settings"` succeeds
3. All 6 files in docker-compose.yml are parseable
4. .env.example contains all vars from research
</verification>

<success_criteria>
- docker-compose.yml defines all 6 services with correct build/command/depends_on
- Dockerfile builds Python 3.11 image with all deps
- pyproject.toml declares all dependencies
- Settings class loads from environment
- All 4 process stubs exist and are importable
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-01-SUMMARY.md` when done
</output>
