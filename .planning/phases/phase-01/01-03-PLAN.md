---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - src/alembic.ini
  - src/alembic/env.py
  - src/alembic/script.py.mako
  - src/alembic/versions/.gitkeep
  - src/app/api/main.py
  - src/app/api/routes/__init__.py
  - src/app/api/routes/health.py
  - src/app/bot/main.py
  - src/app/worker/__init__.py
  - src/app/worker/main.py
  - src/app/worker/settings.py
  - src/app/scheduler/__init__.py
  - src/app/scheduler/main.py
autonomous: true
requirements: []

estimate:
  tokens: 25000
  raw_tokens: 25000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Alembic can autogenerate a migration from the models and apply it to PostgreSQL"
    - "FastAPI health check responds with 200 and database/redis status"
    - "All 4 processes (bot, API, worker, scheduler) start without import errors"
    - "Redis connection is tested and confirmed working"
  artifacts:
    - path: "src/alembic.ini"
      provides: "Alembic configuration pointing to async PostgreSQL"
      contains: "script_location"
    - path: "src/alembic/env.py"
      provides: "Async migration runner using SQLAlchemy 2.0 async engine"
      contains: "run_async"
    - path: "src/app/api/main.py"
      provides: "FastAPI app with lifespan, CORS, health check"
      contains: "FastAPI"
    - path: "src/app/api/routes/health.py"
      provides: "GET /health with DB + Redis status checks"
      contains: "@router.get"
    - path: "src/app/worker/settings.py"
      provides: "ARQ WorkerSettings with Redis config"
      contains: "class WorkerSettings"
  key_links:
    - from: "src/alembic/env.py"
      to: "src/app/shared/database.py"
      via: "Uses same async engine and Base.metadata for autogenerate"
      pattern: "engine"
    - from: "src/alembic/env.py"
      to: "src/app/shared/models/__init__.py"
      via: "Imports Base to get target_metadata"
      pattern: "from app.shared.models import Base"
    - from: "src/app/api/routes/health.py"
      to: "src/app/shared/database.py"
      via: "Tests DB connection with SELECT 1"
      pattern: "session.execute"
    - from: "src/app/api/routes/health.py"
      to: "src/app/shared/config.py"
      via: "Reads REDIS_URL from settings for Redis ping"
      pattern: "settings.REDIS_URL"
---

<objective>
Wire up Alembic migrations, process entrypoints, and health check endpoint — making all services startable and the database migratable.

Purpose: This completes the foundation by connecting all pieces: Alembic can migrate the DB, all 4 processes start cleanly, and health checks verify infrastructure.
Output: A fully functional `docker compose up` where all services start, DB accepts migrations, and health endpoint responds.
</objective>

<execution_context>
@/Users/alekseimikhalkin/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/alekseimikhalkin/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/phases/phase-01/01-01-SUMMARY.md
@.planning/phases/phase-01/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Alembic async migration setup</name>
  <files>src/alembic.ini, src/alembic/env.py, src/alembic/script.py.mako, src/alembic/versions/.gitkeep</files>
  <action>
Configure Alembic for async SQLAlchemy 2.0 migrations:

1. `src/alembic.ini`:
   - script_location = alembic
   - prepend_sys_path = .
   - sqlalchemy.url = (leave empty — env.py reads from settings)
   - Add [loggers] section with keys = root,sqlalchemy,alembic

2. `src/alembic/env.py`:
   - Import asyncio, sys, os from stdlib
   - Add parent dir to sys.path so `app.shared` is importable
   - Import Base from app.shared.models and settings from app.shared.config
   - Import run_async from sqlalchemy.ext.asyncio
   - Import create_async_engine from sqlalchemy.ext.asyncio
   - Set target_metadata = Base.metadata
   - Implement do_run_migrations(config, connection) that runs migrations within connection
   - Implement run_migrations_online() that creates async engine from settings.DATABASE_URL, gets async connection, calls do_run_migrations
   - Use asyncio.run(run_migrations_online()) at module level (not connectable= pattern)
   - IMPORTANT: The env.py must use `connectable = create_async_engine(settings.DATABASE_URL)` and then `async with connectable.connect() as connection:` — this is the standard async Alembic pattern

3. `src/alembic/script.py.mako`:
   - Standard Alembic Mako template with ${message} for revision message
   - Imports: Alembic, sqlalchemy, alembic.op
   - Up/down revision functions

4. `src/alembic/versions/.gitkeep` — empty file to ensure directory exists in git

Use `from __future__ import annotations` where applicable. The env.py file does NOT need it since it runs as a script. Make sure sys.path manipulation puts the src/ directory on the path so `from app.shared.models import Base` works from the alembic directory.
  </action>
  <verify>
    <automated>cd "/Users/alekseimikhalkin/Documents/BOT UILVIYA/src" && python -m alembic heads 2>&1 | head -5 && echo "Alembic configured OK"</automated>
  </verify>
  <done>alembic.ini exists with correct script_location, env.py imports Base.metadata and settings, script.py.mako template exists, versions/ directory has .gitkeep, `alembic heads` runs without import errors.</done>
</task>

<task type="auto">
  <name>Task 2: FastAPI health check and process entrypoints</name>
  <files>src/app/api/main.py, src/app/api/routes/__init__.py, src/app/api/routes/health.py, src/app/bot/main.py, src/app/worker/__init__.py, src/app/worker/main.py, src/app/worker/settings.py, src/app/scheduler/__init__.py, src/app/scheduler/main.py</files>
  <action>
Wire up all 4 process entrypoints and the health check endpoint:

1. `src/app/api/main.py` — Complete FastAPI app:
   - Import FastAPI, CORSMiddleware, lifespan
   - Create async def lifespan(app: FastAPI): startup (log "API starting"), yield, shutdown (log "API stopping")
   - Create app = FastAPI(title="Игрок.Реальность API", version="0.1.0", lifespan=lifespan)
   - Add CORS middleware (allow all origins for dev)
   - Include health router from app.api.routes.health
   - Add `if __name__ == "__main__": uvicorn.run("app.api.main:app", host="0.0.0.0", port=8000)`

2. `src/app/api/routes/__init__.py` — empty

3. `src/app/api/routes/health.py`:
   - Create APIRouter()
   - GET /health endpoint that:
     a. Tests PostgreSQL: `SELECT 1` via async session
     b. Tests Redis: `redis.ping()` via redis.asyncio client
     c. Returns JSON: {"status": "healthy", "database": "ok"|"error", "redis": "ok"|"error"}
   - Import get_session from app.shared.database
   - Import settings from app.shared.config
   - Use `import redis.asyncio as aioredis` for Redis health check
   - Handle exceptions gracefully — if DB fails, return status 503 with database: "error"

4. `src/app/bot/main.py` — Minimal bot startup:
   - Import Bot, Dispatcher from aiogram
   - Import settings from app.shared.config
   - Create bot = Bot(token=settings.BOT_TOKEN)
   - Create dp = Dispatcher()
   - async def main(): log "Bot starting", dp.start_polling(bot)
   - if __name__ == "__main__": asyncio.run(main())

5. `src/app/worker/__init__.py` — empty

6. `src/app/worker/main.py` — ARQ worker entrypoint:
   - Import WorkerSettings from app.worker.settings
   - Import asyncio, sys
   - async def main(): from arq.worker import Worker; await Worker(settings=WorkerSettings).run()
   - if __name__ == "__main__": asyncio.run(main())

7. `src/app/worker/settings.py` — ARQ WorkerSettings class:
   - Import WorkerBaseSettings from arq.worker
   - Import RedisSettings from arq.connections
   - Import settings from app.shared.config
   - class WorkerSettings(WorkerBaseSettings):
     - functions: list = [] (will be populated in Phase 3)
     - cron_jobs: list = [] (will be populated in Phase 3)
     - redis_settings = RedisSettings.from_dsn(settings.REDIS_URL)
     - max_tries = 3
     - retry_delay = 60

8. `src/app/scheduler/__init__.py` — empty

9. `src/app/scheduler/main.py` — APScheduler entrypoint:
   - Import AsyncIOScheduler from apscheduler.schedulers.asyncio
   - Import settings from app.shared.config
   - Create scheduler = AsyncIOScheduler()
   - async def main(): scheduler.start(); await asyncio.Event().wait()  # Block forever
   - if __name__ == "__main__": asyncio.run(main())

Use `import redis.asyncio as aioredis` (NOT `from redis import asyncio`). The health check must handle connection failures gracefully — return error status, not crash. The ARQ WorkerSettings must inherit from WorkerBaseSettings (not just object).
  </action>
  <verify>
    <automated>cd "/Users/alekseimikhalkin/Documents/BOT UILVIYA/src" && python -c "
from app.api.main import app
print(f'FastAPI routes: {[r.path for r in app.routes]}')
assert any('/health' in r.path for r in app.routes), 'No /health route'
print('Health check endpoint registered')
" && python -c "from app.worker.settings import WorkerSettings; print(f'Worker: {len(WorkerSettings.functions)} functions')" && python -c "from app.scheduler.main import scheduler; print(f'Scheduler: {type(scheduler).__name__}')"</automated>
  </verify>
  <done>FastAPI app has /health route, health endpoint tests DB+Redis, bot/worker/scheduler all start without import errors, ARQ WorkerSettings has redis_settings from REDIS_URL.</done>
</task>

<task type="auto">
  <name>Task 3: Verify full stack startup</name>
  <files>docker-compose.yml, src/alembic.ini, src/alembic/env.py, src/app/api/main.py</files>
  <action>
Verify the complete Phase 1 foundation by running the full stack:

1. Start infrastructure services:
   - `docker compose up -d postgres redis`
   - Wait for both to show "healthy" status

2. Run database migrations:
   - `cd src && python -m alembic revision --autogenerate -m "init schema"`
   - `cd src && python -m alembic upgrade head`
   - Verify all 7 tables exist via psql

3. Start API and test health:
   - `docker compose up -d api`
   - `curl http://localhost:8000/health`
   - Verify response: {"status":"healthy","database":"ok","redis":"ok"}

4. Start all services:
   - `docker compose up -d`
   - Verify all 4 application services start without errors
   - Check logs for import errors
  </action>
  <verify>
    <automated>cd "/Users/alekseimikhalkin/Documents/BOT UILVIYA" && docker compose up -d postgres redis && sleep 10 && docker compose ps | grep -E "(postgres|redis)" | grep -q "healthy" && echo "Infrastructure healthy" || echo "Infrastructure not ready"</automated>
  </verify>
  <acceptance_criteria>
- [ ] docker compose ps shows postgres and redis as "healthy"
- [ ] Alembic autogenerate creates a migration with all 7 tables
- [ ] alembic upgrade head applies without errors
- [ ] psql \dt shows: users, referrals, scrolls, payments, user_completions, settings, audit_log
- [ ] curl localhost:8000/health returns 200 with database: "ok" and redis: "ok"
- [ ] All 4 services start with docker compose up (no import errors in logs)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Health endpoint → Infrastructure | Exposes internal service status; must not leak secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-05 | Information Disclosure | health.py | medium | mitigate | Health endpoint returns only "ok"/"error" status, never connection strings or stack traces |
| T-01-06 | Elevation of Privilege | alembic/env.py | high | mitigate | DATABASE_URL must use least-privilege DB user; alembic user needs CREATE/ALTER but not SUPERUSER |
| T-01-07 | Denial of Service | worker/settings.py | low | accept | ARQ retry_delay=60 prevents spam retries; max_tries=3 limits poison pill impact |
</threat_model>

<verification>
1. `alembic revision --autogenerate` produces a migration with all 7 tables
2. `alembic upgrade head` succeeds
3. `curl localhost:8000/health` returns 200 with status, database, redis fields
4. All 4 processes import without errors
5. `docker compose up` starts all services
</verification>

<success_criteria>
- Alembic configured for async PostgreSQL with autogenerate support
- Health check endpoint verifies both PostgreSQL and Redis connectivity
- All 4 processes (bot, API, worker, scheduler) have working entrypoints
- ARQ WorkerSettings configured with Redis connection from settings
- APScheduler configured as standalone singleton process
- Full stack starts with docker compose up
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-03-SUMMARY.md` when done
</output>
