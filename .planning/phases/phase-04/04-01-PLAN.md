---
phase: phase-04
plan: 04-01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/bot/services/progress_service.py
  - src/app/bot/handlers/scroll.py
  - src/app/bot/handlers/__init__.py
autonomous: true
requirements:
  - COMP-01
  - COMP-02
must_haves:
  truths:
    - "Completion callback creates a UserCompletion record with xp_awarded=10"
    - "XP is added to user.xp atomically"
    - "Redis sorted set is updated with user's new XP score"
    - "Double-tap on completion button returns 'Уже отмечено!' without duplicate records"
  artifacts:
    - src/app/bot/services/progress_service.py
  key_links:
    - "scroll.py callback -> progress_service.create_completion -> user.xp update -> Redis zadd"
---

<objective>
Build the progress service layer and wire the scroll completion callback to award XP and update the leaderboard.

Purpose: This is the tracer that proves the completion flow end-to-end — from button tap through DB writes to Redis leaderboard update. Every subsequent plan (streaks, commands) builds on this service layer.

Output: `progress_service.py` with create_completion, add_xp, update_leaderboard. scroll.py callback updated to call the service.
</objective>

<execution_context>
@/Users/alekseimikhalkin/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/alekseimikhalkin/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/phase-04/01-RESEARCH.md

@src/app/shared/models/user.py
@src/app/shared/models/completion.py
@src/app/bot/handlers/scroll.py
@src/app/bot/callbacks/scroll.py
@src/app/bot/services/user_service.py
@src/app/shared/database.py
@src/app/shared/config.py
</context>

<tasks>

<task type="tracer">
  <name>Tracer: Wire completion callback through progress_service to DB + Redis</name>
  <files>src/app/bot/services/progress_service.py, src/app/bot/handlers/scroll.py</files>
  <action>
Create `src/app/bot/services/progress_service.py` with the following async functions:

1. `create_completion(user_id: UUID, scroll_id: UUID, xp: int = 10) -> UserCompletion | None` — Check for existing completion (idempotency via UniqueConstraint on user_id+scroll_id). If exists, return None. Otherwise insert UserCompletion with xp_awarded=xp and return it.

2. `add_xp(user_id: UUID, xp: int) -> int` — Load User by id, increment user.xp by xp, commit, return new total.

3. `update_leaderboard(user_id: UUID, xp: int) -> None` — Connect to Redis via `redis.asyncio.from_url(settings.REDIS_URL)`. Use `zadd` on key `"leaderboard:xp"` with `{str(user_id): xp}`. Close the connection after use. Use a try/finally or async context to ensure cleanup.

Update `src/app/bot/handlers/scroll.py` `handle_scroll_completion` to:
- Accept `session: AsyncSession` (injected by aiogram or use session_factory directly)
- Parse scroll_id from callback data: `scroll_id = callback.data.split(":")[-1]`
- Call `create_completion(user_id=UUID(callback.from_user.id), scroll_id=UUID(scroll_id))`
- If None (already completed), answer "Уже отмечено!" and return
- Call `add_xp(user_id, completion.xp_awarded)`
- Call `update_leaderboard(user_id, new_xp)`
- Answer with f"+{completion.xp_awarded} XP! ⚡"
- Edit message text to "Свиток выполнен! Отличная работа! 🎉" (remove the button via edit_text without reply_markup)

Follow existing service patterns: use `session_factory` from `app.shared.database`, type hints on all functions, docstrings. Use `from __future__ import annotations` at module top.
  </action>
  <verify>
    <automated>cd /Users/alekseimikhalkin/Documents/BOT\ UILVIYA && python -c "from app.bot.services.progress_service import create_completion, add_xp, update_leaderboard; print('import ok')"</automated>
  </verify>
  <done>progress_service.py exports create_completion, add_xp, update_leaderboard. scroll.py callback calls all three in sequence with idempotency guard.</done>
</task>

<task type="auto" tdd="true">
  <name>Progress service unit tests — completion idempotency, XP award, leaderboard write</name>
  <files>tests/unit/test_progress_service.py</files>
  <behavior>
    - create_completion returns UserCompletion when none exists for (user_id, scroll_id)
    - create_completion returns None when completion already exists (idempotency)
    - add_xp increments user.xp and returns new total
    - add_xp with user.xp=0, xp=10 returns 10
    - update_leaderboard calls Redis zadd with correct key and score
  </behavior>
  <action>
Create `tests/unit/test_progress_service.py` with pytest + pytest-asyncio. Mock `session_factory` and Redis client.

Test 1: `test_create_completion_new` — Mock session.execute to return no existing completion. Assert UserCompletion is created with xp_awarded=10 and session.add + session.commit called.

Test 2: `test_create_completion_idempotent` — Mock session.execute to return an existing UserCompletion. Assert create_completion returns None and no insert occurs.

Test 3: `test_add_xp` — Mock session.execute to return a User with xp=0. Call add_xp(user_id, 10). Assert user.xp == 10 and session.commit called.

Test 4: `test_update_leaderboard` — Mock redis.asyncio.from_url to return a mock Redis. Call update_leaderboard. Assert zadd called with key="leaderboard:xp" and {str(user_id): xp}.

Follow test patterns from existing tests if any exist, otherwise use standard pytest-asyncio with AsyncMock.
  </action>
  <verify>
    <automated>cd /Users/alekseimikhalkin/Documents/BOT\ UILVIYA && python -m pytest tests/unit/test_progress_service.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>All 4 tests pass. Idempotency, XP increment, and Redis write are verified.</done>
  <precondition>pytest and pytest-asyncio are installed (check pyproject.toml or requirements-dev.txt)</precondition>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user→bot callback | User sends callback_data with scroll_id — must validate UUID format |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | scroll.py callback | medium | mitigate | Validate scroll_id is valid UUID before DB lookup. Reject malformed IDs with error answer. |
| T-04-02 | Elevation of Privilege | progress_service.add_xp | low | accept | XP is additive only; no negative XP path exists. Race condition on concurrent completions is prevented by UniqueConstraint. |
</threat_model>

<verification>
1. `python -c "from app.bot.services.progress_service import create_completion, add_xp, update_leaderboard"` succeeds
2. `pytest tests/unit/test_progress_service.py -v` passes all 4 tests
3. scroll.py callback handler imports progress_service and calls all 3 functions
</verification>

<success_criteria>
- progress_service.py exists with 3 exported functions
- scroll.py callback performs idempotency check, XP award, leaderboard update
- All unit tests pass
</success_criteria>

<output>
Create `.planning/phases/phase-04/04-01-SUMMARY.md` when done
</output>
