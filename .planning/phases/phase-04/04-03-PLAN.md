---
phase: phase-04
plan: 04-03
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/app/bot/handlers/progress.py
  - src/app/bot/handlers/leaderboard.py
  - src/app/bot/handlers/__init__.py
  - src/app/bot/keyboards/leaderboard.py
autonomous: true
requirements:
  - COMP-06
  - COMP-07
must_haves:
  truths:
    - "/progress command shows user's XP, streak, and completions count"
    - "/leaderboard command shows top 10 users ranked by XP"
    - "Leaderboard data comes from Redis sorted set (fast read)"
    - "Leaderboard updates in real-time after each completion"
  artifacts:
    - src/app/bot/handlers/progress.py
    - src/app/bot/handlers/leaderboard.py
    - src/app/bot/keyboards/leaderboard.py
  key_links:
    - "/progress -> progress_service.get_user_stats -> formatted message"
    - "/leaderboard -> Redis zrevrange -> formatted ranked list"
---

<objective>
Build the /progress and /leaderboard bot commands, plus a leaderboard keyboard for quick access.

Purpose: Users need to see their own stats and compare with others. /progress is personal introspection; /leaderboard is social motivation. Both read from fast data sources (PostgreSQL for personal stats, Redis for leaderboard).

Output: Two new handler modules, one keyboard, registered in handlers/__init__.py.
</objective>

<execution_context>
@/Users/alekseimikhalkin/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/alekseimikhalkin/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/phase-04/01-RESEARCH.md

@src/app/bot/services/progress_service.py
@src/app/bot/services/user_service.py
@src/app/bot/handlers/registration.py
@src/app/shared/models/user.py
@src/app/shared/models/completion.py
@src/app/shared/database.py
@src/app/shared/config.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>/progress command — show user's XP, streak, completions count</name>
  <files>src/app/bot/handlers/progress.py, src/app/bot/services/progress_service.py</files>
  <behavior>
    - /progress shows: "Твой прогресс:\n⚡ XP: {xp}\n🔥 Серия: {streak} дней\n✅ Выполнено: {completions} свитков"
    - User not found: shows "Сначала зарегистрируйся через /start"
    - User with 0 completions: shows streak=0, completions=0 correctly
  </behavior>
  <action>
Add `get_user_stats(user_id: UUID) -> dict` to `src/app/bot/services/progress_service.py`:
- Load User by id
- Count UserCompletion records for this user
- Return `{"xp": user.xp, "streak": user.streak, "completions": count}`

Create `src/app/bot/handlers/progress.py`:
- Import Router, Command, Message
- Create `progress_router = Router(name="progress")`
- Register `@progress_router.message(Command("progress"))` handler
- Handler loads user via `get_user_by_telegram_id(callback.from_user.id)` from user_service
- If user is None, answer "Сначала зарегистрируйся через /start" and return
- Call `get_user_stats(user.id)` from progress_service
- Format and answer with the progress text

Update `src/app/bot/handlers/__init__.py` to export `progress_router`.

Follow the registration.py handler pattern: Router named, Command filter, async handler with type hints.
  </action>
  <verify>
    <automated>cd /Users/alekseimikhalkin/Documents/BOT\ UILVIYA && python -c "from app.bot.handlers.progress import progress_router; print('progress handler ok')"</automated>
  </verify>
  <done>/progress command handler exists, calls get_user_stats, displays XP/streak/completions. Registered in handlers/__init__.py.</done>
</task>

<task type="auto" tdd="true">
  <name>/leaderboard command — top 10 users by XP from Redis sorted set</name>
  <files>src/app/bot/handlers/leaderboard.py, src/app/bot/keyboards/leaderboard.py, src/app/bot/handlers/__init__.py</files>
  <behavior>
    - /leaderboard shows top 10 users ranked by XP with medal emojis for top 3
    - Empty leaderboard: shows "Пока нет данных. Выполняй свитки чтобы попасть в рейтинг!"
    - User's own rank shown if not in top 10
    - Leaderboard keyboard has "🔄 Обновить" button that re-runs /leaderboard
  </behavior>
  <action>
Add `get_leaderboard(top_n: int = 10) -> list[dict]` to `src/app/bot/services/progress_service.py`:
- Connect to Redis via `redis.asyncio.from_url(settings.REDIS_URL)`
- Use `zrevrange("leaderboard:xp", 0, top_n - 1, withscores=True)`
- Return list of `{"user_id": uid.decode(), "xp": int(score)}` dicts
- Add `get_user_rank(user_id: UUID) -> int | None` that uses `zrevrank` to get the user's position (0-indexed)

Create `src/app/bot/keyboards/leaderboard.py`:
- Build InlineKeyboard with "🔄 Обновить" button using a new CallbackData factory
- Create `LeaderboardRefresh(CallbackData, prefix="lb")` in callbacks

Create `src/app/bot/handlers/leaderboard.py`:
- Import Router, Command, Message, CallbackQuery
- Create `leaderboard_router = Router(name="leaderboard")`
- Register `@leaderboard_router.message(Command("leaderboard"))` handler
- Call `get_leaderboard(10)` from progress_service
- Format ranked list with medal emojis: 🥇 for #1, 🥈 for #2, 🥉 for #3, numbers for rest
- For each entry, look up user's first_name via `get_user_by_id(UUID(user_id))` from user_service
- If leaderboard is empty, show hint message
- Attach leaderboard keyboard with refresh button
- Register callback handler for LeaderboardRefresh to re-send the leaderboard

Update `src/app/bot/handlers/__init__.py` to export `leaderboard_router`.

For the user rank display: after showing top 10, call `get_user_rank(callback.from_user.id)`. If rank >= 10, append "\n\nТы на позиции #{rank+1}" to the message.
  </action>
  <verify>
    <automated>cd /Users/alekseimikhalkin/Documents/BOT UILVIYA && python -c "from app.bot.handlers.leaderboard import leaderboard_router; print('leaderboard handler ok')"</automated>
  </verify>
  <done>/leaderboard command shows top 10 from Redis, formatted with medals. Refresh button works. User rank shown if outside top 10. Both routers registered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Redis read | Leaderboard data from Redis — untrusted if Redis is compromised |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-04 | Information Disclosure | /leaderboard | low | accept | Leaderboard shows only first_name and XP — no sensitive data. Public by design. |
| T-04-05 | Denial of Service | Redis connection | low | accept | Redis is local (same Docker network). Connection failure shows "Данные временно недоступны" fallback message. |
</threat_model>

<verification>
1. `python -c "from app.bot.handlers.progress import progress_router"` succeeds
2. `python -c "from app.bot.handlers.leaderboard import leaderboard_router"` succeeds
3. Both routers are exported from handlers/__init__.py
4. `pytest tests/unit/test_progress_service.py tests/unit/test_streak.py -v` still passes (no regressions)
</verification>

<success_criteria>
- /progress shows XP, streak, completions from PostgreSQL
- /leaderboard shows top 10 from Redis sorted set with medals
- Refresh button re-queries Redis
- User's own rank displayed if outside top 10
- Both handlers registered in handlers/__init__.py
</success_criteria>

<output>
Create `.planning/phases/phase-04/04-03-SUMMARY.md` when done
</output>
