---
phase: phase-06
plan: 06-02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/app/bot/handlers/referral.py
  - src/app/bot/handlers/__init__.py
  - src/app/bot/main.py
  - src/app/shared/config.py
  - src/app/bot/services/commission.py
  - tests/unit/test_referral_handler.py
autonomous: true
requirements:
  - REF-01
  - REF-02
  - REF-04
  - REF-05
must_haves:
  truths:
    - "User sends /referral and sees their unique referral link"
    - "Referral link format is https://t.me/BOT_USERNAME?start=REFERRAL_CODE"
    - "Commission rate is configurable via settings.COMMISSION_RATE"
    - "Referee receives notification when they join via referral link (REF-04)"
    - "Referral record is created on user registration via deep link"
    - "Referrer receives commission notification on referee's first payment"
  artifacts:
    - src/app/bot/handlers/referral.py
    - src/app/shared/config.py (updated)
  key_links:
    - "/referral handler -> user.get_user_by_telegram_id -> referral link display"
    - "registration flow -> user_service.create_referral -> Referral record created"
    - "settings.COMMISSION_RATE -> commission.calculate_commission"
---

<objective>
Add /referral command handler, configurable commission rate, and complete referral tracking on registration.

Purpose: Users need a way to view and share their referral link. The referral tracking must be completed end-to-end (Referral record created on /start with deep link). Commission rate must be configurable, not hardcoded.

Output: /referral handler with inline keyboard, Referral record creation in registration flow, configurable COMMISSION_RATE in settings.
</objective>

<execution_context>
@/Users/alekseimikhalkin/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/alekseimikhalkin/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/phase-06/01-RESEARCH.md
@.planning/phases/phase-06/06-01-SUMMARY.md

@app/shared/models/user.py
@app/shared/models/commission.py
@app/shared/config.py
@app/bot/services/user_service.py
@app/bot/services/commission.py
@app/bot/handlers/__init__.py
@app/bot/main.py
@app/shared/database.py
</context>

<tasks>

<task type="tracer">
  <name>Tracer: /referral handler, Referral record creation, configurable rate</name>
  <files>src/app/bot/handlers/referral.py, src/app/bot/handlers/__init__.py, src/app/bot/main.py, src/app/shared/config.py, src/app/bot/services/commission.py</files>
  <action>
Create the following:

1. `src/app/bot/handlers/referral.py` — Referral handler:
   - `referral_router = Router(name="referral")`
   - `@referral_router.message(Command("referral"))` async handler:
     a. Get user via get_user_by_telegram_id(message.from_user.id)
     b. If user is None, answer "Сначала зарегистрируйтесь: /start"
     c. If user.referral_code is None, answer "У вас нет реферального кода"
     d. Build referral link: f"https://t.me/{settings.BOT_USERNAME}?start={user.referral_code}"
     e. Answer with text showing the link and instructions:
        "Ваша реферальная ссылка:\n{link}\n\nПоделитесь с друзьями! За каждого оплатившего реферала вы получаете комиссию."
     f. Add InlineKeyboard with button "Скопировать ссылку" using callback_data or CopyTextButton
   - Import: Router, Message, Command from aiogram, get_user_by_telegram_id, settings

2. Update `src/app/bot/handlers/__init__.py`:
   - Add `from app.bot.handlers.referral import referral_router`
   - Export referral_router

3. Update `src/app/bot/main.py`:
   - Add `dp.include_router(referral_router)` after existing routers

4. Update `src/app/shared/config.py`:
   - Add `COMMISSION_RATE: float = 0.10` field to Settings class (after PLATEGA_WEBHOOK_URL)
   - This replaces the hardcoded COMMISSION_RATE in commission.py

5. Update `src/app/bot/services/commission.py`:
   - Remove `COMMISSION_RATE = 0.10` constant
   - Import settings from app.shared.config
   - Change `commission = int(payment.amount * COMMISSION_RATE)` to `commission = int(payment.amount * settings.COMMISSION_RATE)`

6. Update registration flow to create Referral record and notify referee:
   - In `src/app/bot/services/user_service.py`, modify `create_user`:
     a. After creating the User record, check if `started_by` parameter is provided (the referrer's referral code)
     b. If referral_code was passed to create_user, call create_referral(referral_code, user.id)
     c. This links the referrer to the referee in the Referral table
   - The /start handler in registration.py already parses the deep link — ensure it passes the referral_code to create_user
   - **Referee notification (REF-04):** In `src/app/bot/handlers/registration.py`, after creating the referral:
     a. Look up the referrer by referral_code
     b. Send notification to referee: "Вы приглашены {referrer.first_name}! Добро пожаловать в квест!"
     c. This is already partially implemented in handle_q4 — verify it works correctly
  </action>
  <verify>
    <automated>cd /Users/alekseimikhalkin/Documents/BOT\ UILVIYA && python -c "from app.bot.handlers.referral import referral_router; from app.shared.config import settings; assert hasattr(settings, 'COMMISSION_RATE'); print('import ok')"</automated>
  </verify>
  <done>/referral handler exists and registers with bot. Referral link is generated from user's referral_code. COMMISSION_RATE is configurable in settings. create_user creates Referral record when referral_code is provided. Import check passes.</done>
  <precondition>Plan 06-01 is complete — CommissionBalance model and enhanced commission.py exist</precondition>
  <reversibility rating="reversible">Adding a new handler and config field is non-breaking. Removing them reverts to no /referral command.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Referral handler unit tests and configurable rate tests</name>
  <files>tests/unit/test_referral_handler.py</files>
  <behavior>
    - /referral command shows user their referral link
    - /referral link format is correct (https://t.me/BOT_USERNAME?start=CODE)
    - /referral before registration returns error
    - COMMISSION_RATE from settings is used in commission calculation
    - create_user with referral_code creates Referral record
  </behavior>
  <action>
Create `tests/unit/test_referral_handler.py` following patterns from test_leaderboard_handler.py.

Test 1: `test_referral_shows_link` — Mock get_user_by_telegram_id to return User with referral_code="abc12345". Mock settings.BOT_USERNAME="testbot". Send /referral message. Assert response contains "https://t.me/testbot?start=abc12345".

Test 2: `test_referral_before_registration` — Mock get_user_by_telegram_id to return None. Send /referral message. Assert response contains "зарегистрируйтесь".

Test 3: `test_commission_rate_configurable` — Mock settings.COMMISSION_RATE=0.15. Mock payment.amount=10000. Call calculate_commission. Assert commission amount is 1500 (15% of 10000).

Test 4: `test_create_user_with_referral_code` — Mock session_factory. Call create_user with referral_code="ref123". Assert Referral record created with correct referrer_id and referee_id.

Use pytest, pytest-asyncio, unittest.mock.AsyncMock/MagicMock.
  </action>
  <verify>
    <automated>cd /Users/alekseimikhalkin/Documents/BOT\ UILVIYA && python -m pytest tests/unit/test_referral_handler.py -v 2>&1 | tail -15</automated>
  </verify>
  <done>All 4 tests pass. /referral handler displays correct link. Configurable rate works. Referral record created on registration.</done>
  <precondition>/referral handler and updated commission.py exist from Task 1</precondition>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user→bot /referral | User requests their referral link — must validate registration |
| settings→commission | COMMISSION_RATE read from settings — must validate range |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-04 | Information Disclosure | /referral handler | low | accept | Referral link is non-sensitive (Telegram deep link). No private data exposed. |
| T-06-05 | Tampering | COMMISSION_RATE setting | medium | mitigate | Rate validated as float 0.0-1.0. Future: admin-only editable via admin panel. |
| T-06-06 | Elevation of Privilege | Referral self-referral | low | accept | create_referral already checks referrer.id != referee.id (existing guard in user_service.py) |
</threat_model>

<verification>
1. `python -c "from app.bot.handlers.referral import referral_router"` succeeds
2. `python -c "from app.shared.config import settings; print(settings.COMMISSION_RATE)"` returns 0.10
3. `pytest tests/unit/test_referral_handler.py -v` passes all 4 tests
4. Bot main.py includes referral_router
</verification>

<success_criteria>
- /referral handler shows user's unique referral link
- Referral link format is https://t.me/BOT_USERNAME?start=CODE
- COMMISSION_RATE is configurable in settings (default 0.10)
- Referral record created on user registration via deep link
- All unit tests pass
</success_criteria>

<output>
Create `.planning/phases/phase-06/06-02-SUMMARY.md` when done
</output>
