---
phase: phase-06
plan: 06-01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/shared/models/commission.py
  - src/app/shared/models/__init__.py
  - src/alembic/versions/20260905_add_commission_balances.py
  - src/app/bot/services/commission.py
  - tests/unit/test_commission_balance.py
autonomous: true
requirements:
  - REF-03
  - REF-06
must_haves:
  truths:
    - "CommissionBalance model stores earned, pending, and paid_out amounts per user"
    - "Alembic migration creates commission_balances table"
    - "One-time commission: only first succeeded payment per referee triggers commission"
    - "calculate_commission persists results to CommissionBalance"
    - "Webhook CONFIRMED handler calls updated calculate_commission which writes balance"
  artifacts:
    - src/app/shared/models/commission.py
    - src/alembic/versions/20260905_add_commission_balances.py
    - src/app/bot/services/commission.py
  key_links:
    - "calculate_commission -> CommissionBalance.upsert -> session.commit"
    - "webhook CONFIRMED -> calculate_commission -> balance updated"
---

<objective>
Create CommissionBalance model and enhance commission service with balance persistence and one-time commission logic.

Purpose: This is the tracer that proves commission tracking end-to-end — from payment webhook through calculation to persistent balance storage. The model and service are prerequisites for referral handler and admin payout.

Output: CommissionBalance model, Alembic migration, enhanced commission.py with balance writes and one-time-first-payment check.
</objective>

<execution_context>
@/Users/alekseimikhalkin/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/alekseimikhalkin/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/phase-06/01-RESEARCH.md

@app/shared/models/user.py
@app/shared/models/payment.py
@app/shared/models/base.py
@app/shared/models/audit.py
@app/shared/models/__init__.py
@app/shared/database.py
@app/bot/services/commission.py
@app/api/webhooks.py
@app/bot/services/user_service.py
</context>

<tasks>

<task type="tracer">
  <name>Tracer: CommissionBalance model, migration, and enhanced commission service</name>
  <files>src/app/shared/models/commission.py, src/app/shared/models/__init__.py, src/alembic/versions/20260905_add_commission_balances.py, src/app/bot/services/commission.py</files>
  <action>
Create the following following existing patterns:

1. `src/app/shared/models/commission.py` — New model file:
   - `class CommissionBalance(Base, UUIDPrimaryKeyMixin, TimestampMixin)`:
     - `__tablename__ = "commission_balances"`
     - `user_id: Mapped[UUID]` — ForeignKey("users.id"), unique=True, indexed
     - `total_earned: Mapped[int]` — Integer, default=0, kopecks
     - `total_pending: Mapped[int]` — Integer, default=0, kopecks
     - `total_paid_out: Mapped[int]` — Integer, default=0, kopecks
     - `last_commission_at: Mapped[Optional[datetime]]` — DateTime(timezone=True), nullable
   - Imports: Base, TimestampMixin, UUIDPrimaryKeyMixin from base, ForeignKey from sqlalchemy, Mapped/mapped_column from sqlalchemy.orm

2. Update `src/app/shared/models/__init__.py`:
   - Add `from app.shared.models.commission import CommissionBalance`
   - Add "CommissionBalance" to `__all__`

3. Create `src/alembic/versions/20260905_add_commission_balances.py`:
   - Follow pattern from `20260905_add_started_at_to_users.py`
   - revision = "a1b2c3d4e5f6" (generate unique)
   - Create `commission_balances` table with all columns
   - FK to users.id, unique constraint on user_id

4. Enhance `src/app/bot/services/commission.py`:
   - Add import for CommissionBalance, select, session_factory
   - Add `async def _get_or_create_balance(session, user_id: UUID) -> CommissionBalance` — query CommissionBalance by user_id, create if not exists with defaults
   - Modify `calculate_commission`:
     a. After loading the paying user, check if this is the FIRST succeeded payment:
        - Query: `select(Payment).where(Payment.user_id == user.id, Payment.status == "succeeded")`
        - If count > 1 (this payment is not the first), return {"amount": 0, "mentor_id": None, "mentor_telegram_id": None}
        - This implements one-time commission per referee (user decision)
     b. After calculating commission amount, persist to balance:
        - Open a new session, call _get_or_create_balance for mentor.id
        - Increment total_pending by commission amount
        - Set last_commission_at = datetime.now(timezone.utc)
        - Commit
     c. Return the same dict as before (amount, mentor_id, mentor_telegram_id)
   - Keep COMMISSION_RATE as module constant for now (becomes configurable in Plan 06-02)
   - The webhook handler in webhooks.py needs no changes — it already calls calculate_commission
  </action>
  <verify>
    <automated>cd /Users/alekseimikhalkin/Documents/BOT\ UILVIYA && python -c "from app.shared.models.commission import CommissionBalance; from app.bot.services.commission import calculate_commission; print('import ok')"</automated>
  </verify>
  <done>CommissionBalance model exists and is registered in __init__.py. Alembic migration creates the table. calculate_commission checks for first succeeded payment (one-time commission) and persists balance to CommissionBalance. Import check passes.</done>
  <precondition>Phase 5 is complete — commission.py and webhook handler exist and work</precondition>
</task>

<task type="auto" tdd="true">
  <name>Commission balance unit tests — one-time logic and balance persistence</name>
  <files>tests/unit/test_commission_balance.py</files>
  <behavior>
    - calculate_commission returns amount > 0 for first succeeded payment with mentor
    - calculate_commission returns amount=0 for second succeeded payment (one-time enforcement)
    - calculate_commission returns amount=0 when user has no mentor
    - CommissionBalance record is created on first commission
    - CommissionBalance.total_pending increments on each commission
    - last_commission_at is set on commission
  </behavior>
  <action>
Create `tests/unit/test_commission_balance.py` following patterns from test_commission.py.

Test 1: `test_one_time_commission_first_payment` — Mock session_factory to return: user with referred_by_id, mentor user, one succeeded payment. Call calculate_commission. Assert amount > 0. Assert session.add called with CommissionBalance.

Test 2: `test_one_time_commission_second_payment_no_commission` — Mock session_factory to return: user with referred_by_id, mentor user, two succeeded payments (the current one + a prior one). Call calculate_commission. Assert amount == 0. Assert session.add NOT called.

Test 3: `test_commission_no_mentor` — Mock session_factory to return: user with referred_by_id=None. Call calculate_commission. Assert amount == 0.

Test 4: `test_balance_created_on_first_commission` — Mock session_factory. Call calculate_commission for first payment. Assert CommissionBalance created with total_pending=commission_amount.

Test 5: `test_balance_increments_on_subsequent_commission` — Mock session_factory to return existing CommissionBalance. Call calculate_commission. Assert total_pending increased by commission amount.

Use pytest, pytest-asyncio, unittest.mock.AsyncMock/MagicMock. Follow test_commission.py patterns.
  </action>
  <verify>
    <automated>cd /Users/alekseimikhalkin/Documents/BOT\ UILVIYA && python -m pytest tests/unit/test_commission_balance.py -v 2>&1 | tail -15</automated>
  </verify>
  <done>All 5 tests pass. One-time commission and balance persistence logic verified.</done>
  <precondition>CommissionBalance model and enhanced commission.py exist from Task 1</precondition>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| webhook→commission | Payment webhook triggers commission calculation — must validate payment status |
| commission→balance | Commission writes to CommissionBalance — must be atomic |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Tampering | commission._get_or_create_balance | medium | mitigate | Use SELECT FOR UPDATE pattern to prevent race conditions on balance creation |
| T-06-02 | Elevation of Privilege | calculate_commission | medium | mitigate | One-time check queries payment count before granting — prevents repeated commissions |
| T-06-03 | Denial of Service | CommissionBalance creation | low | accept | Rate limited by Telegram payment flow — not exploitable |
</threat_model>

<verification>
1. `python -c "from app.shared.models.commission import CommissionBalance"` succeeds
2. `python -c "from app.bot.services.commission import calculate_commission"` succeeds
3. `pytest tests/unit/test_commission_balance.py -v` passes all 5 tests
4. Alembic migration file exists and is syntactically valid
</verification>

<success_criteria>
- CommissionBalance model with earned/pending/paid_out fields exists
- Alembic migration creates commission_balances table
- calculate_commission enforces one-time commission (first payment only)
- calculate_commission persists to CommissionBalance on commission
- All unit tests pass
</success_criteria>

<output>
Create `.planning/phases/phase-06/06-01-SUMMARY.md` when done
</output>
